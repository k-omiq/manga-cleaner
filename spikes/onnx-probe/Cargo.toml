[package]
name = "spike-onnx-probe"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

# Phase 0 spikes 2 and 6: what a model's tensor contract actually is, and
# what it costs to run.
[dependencies]
cleaner-core = { path = "../../crates/cleaner-core" }
ort = { workspace = true, features = ["std", "load-dynamic", "api-22", "coreml"] }
anyhow.workspace = true
