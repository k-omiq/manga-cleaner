[package]
name = "spike-gate-probe"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

# The balloon question, region by region, on a real page: what the balloon
# detector said, what the paper reading said, and what the gate concluded. One
# line per region, so that a page whose dialogue lands in review under "text
# outside a speech bubble" can be read back to the mechanism that put it there.
[dependencies]
cleaner-core = { path = "../../crates/cleaner-core" }
anyhow.workspace = true
